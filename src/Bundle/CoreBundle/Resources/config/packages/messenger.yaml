framework:
    messenger:
        buses:
            command_bus:
                middleware:
                  - message_middleware
                  - doctrine_transaction_middleware
                  - allow_no_handler
