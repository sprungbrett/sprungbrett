<h1>{{ content.title }}</h1>

{{ content.article|raw }}

<ul>
    {% for item in courses %}
        <li><a href="{{ sulu_content_path(item.route) }}">{{ item.course.name }}</a></li>
    {% endfor %}
</ul>
